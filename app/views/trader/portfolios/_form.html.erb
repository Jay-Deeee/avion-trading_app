<%= form_with url: trader_add_balance_path, method: :post, local: true do |form| %>
    <%= form.number_field :amount, step: 0.01, min: 0, placeholder: "Amount" %>
    <%= form.submit "Add Balance", class: "btn btn-success" %>
  <% end %>